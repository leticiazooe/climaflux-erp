{
  "name": "climaflux-erp",
  "version": "0.6.0",
  "private": true,
  "description": "ERP demonstrativo para assistência técnica em ar-condicionado e refrigeração, com SLA, despacho técnico e portal do cliente.",
  "type": "module",
  "scripts": {
    "build:public": "node scripts/build-public.mjs",
    "serve": "npm run build:public && python3 -m http.server 8080 --directory public",
    "start": "npm run serve",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.119.0"
  },
  "engines": {
    "node": ">=18"
  }
}
