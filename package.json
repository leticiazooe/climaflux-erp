{
  "name": "climaflux-erp",
  "version": "0.7.0",
  "private": true,
  "description": "ERP demonstrativo para assistência técnica em ar-condicionado e refrigeração.",
  "type": "module",
  "scripts": {
    "build:public": "node scripts/build-public.mjs",
    "serve": "npm run build:public && python3 -m http.server 8080 --directory public",
    "start": "npm run serve",
    "preview": "wrangler dev",
    "deploy": "wrangler deploy",
    "test:auth": "node --test tests/auth.test.mjs",
    "check:auth": "node --check worker/index.js && node --check worker/auth-core.js && node --check auth/login.js && node --check auth/auth-client.js && node --check auth/admin-access.js && node --check auth/secure-sw.js",
    "auth:db:create": "wrangler d1 create climaflux-auth",
    "auth:migrate:local": "wrangler d1 migrations apply DB --local",
    "auth:migrate": "wrangler d1 migrations apply DB --remote"
  },
  "devDependencies": {
    "wrangler": "4.119.0"
  },
  "engines": {
    "node": ">=24"
  }
}
